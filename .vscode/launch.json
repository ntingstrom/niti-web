{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "▶️ Start: npm run dev",
            "type": "pwa-node",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}